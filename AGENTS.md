# 网站维护规则

## WU Logo（固定规范）

- 全站所有 WU Logo 必须复用 `index.html` 中的 `.wu-wordmark` 结构和公共样式，顶部导航与页脚使用同一组件；不得再用纯文本 `WU` 或另写一套近似 Logo。
- 固定构成为衬线粗体 `WU` 加位于 `U` 右下方的哑光金色小方块。WU 使用深金色 `#806B48`，方块使用主金色 `#A78E62`；不得更换字体、颜色、形状或相对位置。
- Logo 自身必须为右下方方块预留布局空间，不得依靠压缩相邻文字、负间距或临时位移规避重叠。
- 响应式布局只允许整体等比例缩放；顶部与页脚在同一视口下必须保持相同字号、比例与方块位置。
- 每次修改 Logo、导航或页脚后，必须检查约 1280px、600px 和 375px 三种宽度，确认 Logo、品牌名和导航无重叠、裁切或横向溢出。

## PC 与手机适配（用户明确要求）

- 每次更新网站内容、图片或布局，都必须同时考虑 PC 和手机端的显示效果。
- 完成前检查宽屏（约 1280px）、窄窗口（约 600px）和手机（约 375px）下受影响的页面或组件；涉及断点时检查断点两侧。
- 不允许因固定高度、跨行网格、拉伸对齐、图片尺寸或内容长短不一致产生大块无意义空白。根据内容调整尺寸、间距和排列；需要补图时使用与内容相关的配图。
- 同一行并排展示的成组卡片（特别是 AI 知识树与学习概览）必须顶部和底部对齐。通过弹性布局和合理的内容间距填充内部空间，不能只拉高外框却留下大片空白；手机端改为上下排列时按内容自然定高，不强制等高。
- 检查标题、Logo、导航、图片与控件是否重叠、溢出、被裁切或挤压；手机端文字应可读，交互控件应可操作。
- 保留用户已经确认的设计：工作电台为左侧播放器、右侧歌单；通过紧凑尺寸适配窄屏，不擅自改回上下布局。
- 使用真实内容验证适配效果，不仅检查占位文本。未完成相应屏幕尺寸的验证时，不宣称已适配。
- 保留用户素材与其他任务的未提交改动；发布前核对最新远端，避免覆盖并行更新。
